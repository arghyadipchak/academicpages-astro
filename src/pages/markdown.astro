---
import { getEntry, render } from 'astro:content';
import SingleLayout from '../layouts/SingleLayout.astro';

const guidePage = await getEntry('pages', 'markdown');
if (!guidePage) {
  throw new Error("Missing 'markdown' page in content/pages");
}

const { Content } = await render(guidePage);
---

<SingleLayout
  title={guidePage.data.title}
  author_profile={guidePage.data.author_profile}
>
  <Content />
</SingleLayout>
